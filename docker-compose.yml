version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI="mongodb+srv://saikumar:abc12345@cluster0.281meha.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0"
      - JWT_SECRET=KUNFU
      - JWT_EXPIRE=30d
      - NODE_ENV=production
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
